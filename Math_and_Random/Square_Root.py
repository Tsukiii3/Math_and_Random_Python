import math

n = int(input('Enter a number'))
rt = math.sqrt(n) #Calculate the square root using the math method.
print('The square root of {} is equal to {:.2f}'.format(n,rt)) # :.2f Displays only 2 decimal places